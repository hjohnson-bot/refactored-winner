# Reporting Inventory — Cover Memo

**To:** Steve Little
**From:** Hunter Johnson
**Date:** 2026-05-27
**Re:** Reporting inventory & streamlining opportunities (per your post-lunch email)

> _Draft generated by cloud scaffolding session. Numbers in this memo are
> placeholders until the local ingestion session completes._

---

## 1. Scope & methodology

Per your request I compiled every recurring report, projection, analysis,
and spreadsheet model I currently produce across **TI (Tenant Improvement)**,
**MF (Multi-Family)**, and the Company rollup. For each item I captured the
seven fields you specified — requester, purpose, frequency, time commitment,
data/output ownership, classification, and overlap — plus a few supporting
fields (entity, source system, delivery format, streamlining opportunity)
that should help us prioritize.

Method:
1. **Source discovery** — scanned my local working directories, Claude
   chat memory, Knowify/QuickBooks exports, my calendar, and the
   `cfo-dashboard/` codebase to enumerate every artifact.
2. **Per-report walk** — built one entry per report using a fixed template
   so the data is sortable and filterable.
3. **Overlap pass** — second pass across the full set to flag duplicate
   work and consolidation candidates.

Deliverables in `reporting-inventory/drafts/`:
- `inventory.xlsx` — sortable Excel workbook (the working file)
- `inventory.csv` — same data, drop-in for Google Sheets
- `cover-memo.pdf` — this document

---

## 2. Headline numbers

> _Populated by local ingestion session._

| Metric | Value |
|---|---|
| Total reports inventoried | TBD |
| 🔴 Operationally critical | TBD |
| 🟡 Informational | TBD |
| ⚪ Discretionary | TBD |
| Estimated hours per month, all reports | TBD |
| Reports with known overlap / duplication | TBD |
| Top concentration of time | TBD (e.g., "monthly close pack consumes ~X hrs") |

---

## 3. Top streamlining opportunities (preliminary)

Ranked by rough hours-recovered-per-month, refined after the full ingestion:

1. **Automate the monthly QBO snapshot refresh.** The `cfo-dashboard/`
   pipeline already does the heavy lifting; scheduling a Claude Code
   session on the 1st to run `refresh.sh prompt → build` and commit the
   regenerated `snapshot.json` removes a recurring manual loop.
2. **Retire the per-month QB query workaround when upstream is fixed.**
   The current loop exists because QBO MCP's `monthlyBreakdown` doesn't
   reconcile to its own annual totals. Track quarterly; flip the switch
   once it does.
3. **Host the CFO dashboard so consumers self-serve.** Eliminates the
   recurring "send me a screenshot of …" requests.
4. **Auto-digest the Action Flags + Forecast on snapshot refresh.**
   Push the critical signal to Steve's inbox without needing to ask.
5. **Decide TI / MF segment reporting strategy.** _TBD_ — depends on
   whether QBO Class is already populated for TI vs MF. If yes, the
   existing dashboard class filter just needs data. If no, that's the
   real conversation: instrument first, report second.

---

## 4. 5 talking points for our check-in today

1. **Confirm scope.** Are we inventorying only my recurring outputs, or
   also one-off analyses I've delivered in the last ~90 days?
2. **Confirm completion date.** I'm proposing a polished v1 by
   end-of-day **2026-06-03** (5 business days), with the seeded
   `inventory.md` available immediately for your review.
3. **Confirm format priority.** I'm preparing all three (Excel, Google
   Sheet, PDF memo). Which do you want to anchor on?
4. **TI / MF segmentation.** Do we have reliable QBO Class data tagged
   TI vs MF today? If not, that gap blocks segment-level reporting and
   should probably move ahead of further report consolidation.
5. **Streamlining authority.** Of the opportunities in §3, which can I
   action unilaterally, which need your sign-off, and which need a
   partner-group conversation?

---

## 5. Proposed next steps

- **Today, after our check-in:** I lock scope and completion date.
- **By end-of-day 2026-05-28:** Local ingestion session complete —
  every report from the last 90 days captured in `inventory.md`.
- **By 2026-05-30:** First-pass draft sent to you for review.
- **By 2026-06-03:** Final Excel + PDF + Google Sheet delivered.

---

_Working files: `reporting-inventory/` on branch
`claude/reporting-inventory-analysis-oEMkp`._
